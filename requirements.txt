# PRO Trader — protrader-server requirements
#
# Built directly from the actual import/from statements confirmed present
# in server.py — nothing added speculatively. gunicorn is here even though
# it's never imported in the code itself — it's invoked directly in the
# start command (gunicorn server:app --bind ...), which is exactly why an
# earlier version of this file missed it and caused a real deployment
# failure ("gunicorn: command not found").

flask
flask-cors
requests
pywebpush
supabase
gunicorn
